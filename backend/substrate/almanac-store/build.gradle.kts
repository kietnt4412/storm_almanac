// Track B · Phase 7. Embedded log-structured merge tree.
// Deliberately a plain Java library: no Spring, no framework, no network.
// A finished small engine beats an unfinished ambitious one.
dependencies {
    testImplementation(libs.jqwik)
}
