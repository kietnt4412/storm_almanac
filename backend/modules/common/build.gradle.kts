// Shared kernel: identifiers, versioning, the internal event contract.
// Depends on nothing. Anything that grows a dependency here does not belong here.
dependencies {
    api(libs.springBootStarterValidation)
}
