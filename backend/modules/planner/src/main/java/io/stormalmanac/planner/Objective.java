package io.stormalmanac.planner;

/**
 * Least energy and fewest days are different plans, and a player wants to see
 * the trade rather than be handed one of them. Both are solved and both are
 * returned.
 */
public enum Objective { LEAST_ENERGY, FEWEST_DAYS }
